# --- Conda/Python Environments ---
env/
venv/
.venv/
/anaconda3/
/miniconda3/
*.conda

#Poppler folder for PDF processing
poppler-24.07.0/

# --- Python bytecode and caches ---
__pycache__/
*.py[cod]
*$py.class

# --- IDE specific files ---
.vscode/
.idea/

# --- OS specific files ---
.DS_Store
Thumbs.db

# --- Streamlit specific ---
.streamlit/
# IMPORTANT: Add secrets.toml if using Streamlit secrets!
.streamlit/secrets.toml

# --- Project Specific Files ---
# Uploaded PDFs (as defined in UPLOAD_DIR)
uploaded_pdfs/
*.pdf # Maybe too broad? Use the directory if possible.

# Parsed data cache (as defined in CACHE_DIR)
cache/
*_parsed_cache.pkl

# FAISS Index cache (as defined in FAISS_INDEX_DIR)
faiss_index_cache/
vector_store.faiss
docstore.json
index_store.json
graph_store.json
*.faiss # General pattern for FAISS files

# --- Secrets & Sensitive Configs ---
# IMPORTANT: Add .env if using python-dotenv!
.env
*.env
secrets.yaml
# config.ini # If it contains secrets

# --- Log files ---
*.log

# --- Dependencies (should be in environment.yml) ---
# These might appear if packages are installed locally by mistake
lib/
include/
bin/
Scripts/ # Windows specific

# --- Jupyter Notebook Checkpoints ---
.ipynb_checkpoints/

# --- Asset files (if they are very large, otherwise keep them) ---
# assets/ # Uncomment if your logos/images are huge and users should download them separately

# --- Ghostscript/Tesseract temp files (if any created) ---
# Add patterns if you notice specific temp files being created